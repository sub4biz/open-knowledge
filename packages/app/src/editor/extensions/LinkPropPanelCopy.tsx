/**
 * CopyButton for the link PropPanels.
 *
 * The implementation now lives in the shared `@/components/CopyButton` (also
 * used by the ShareButton popover). This module re-exports it so the existing
 * PropPanel import sites stay stable.
 */

export { CopyButton } from '@/components/CopyButton';
