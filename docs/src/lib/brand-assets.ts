// The /brand page and downloadable brand kit now live in the marketing app
// (served at the apex via Vercel Microfrontends). This constant stays so the
// docs sitemap and footer can keep linking to the brand route.
export const BRAND_ROUTE = '/brand';
